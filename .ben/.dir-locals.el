;; Directory Local Variables
;;; For more information see (info "(emacs) Directory Variables")

((makefile-mode . ((outshine-startup-folded-p . nil)
                   (eval . (progn
                             (outshine-mode 1)
                             (column-enforce-mode 1))))))
